name: live-caption-app

services:
  live-caption:
    image: live-caption:latest
    container_name: live-caption
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - DEEPGRAM_API_KEY
        - ENVIRONMENT
        - BFF_URL
    env_file:
      - .env
    environment:
      NODE_ENV: production
      TZ: America/New_York
    ports:
      - "127.0.0.1:8088:8080"
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    read_only: false
    tmpfs:
      - /tmp
      - /var/run/nginx
      - /var/log/nginx
      - /run
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
